import { ReactComponent as InfoSvg } from '../../assets/images/icons/information.svg';
import { IconProps } from './IconProps';

export default function InfoIcon({ className }: IconProps) {
  return <InfoSvg className={className} />;
}
