import { ReactComponent as GaugeSvg } from '../../assets/images/icons/gauge.svg';

export default function GaugeIcon({ className }: { className: string }) {
  return <GaugeSvg className={className} />;
}
