import { ReactComponent as ReportSvg } from '../../assets/images/icons/report.svg';

export default function ReportIcon({ className }: { className: string }) {
  return <ReportSvg className={className} />;
}
