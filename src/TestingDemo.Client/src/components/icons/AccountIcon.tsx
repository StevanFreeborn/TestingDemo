import { ReactComponent as AccountSvg } from '../../assets/images/icons/account.svg';

export default function AccountIcon({ className }: { className: string }) {
  return <AccountSvg className={className} />;
}
