import { ReactComponent as ContentSvg } from '../../assets/images/icons/content.svg';

export default function ContentIcon({ className }: { className: string }) {
  return <ContentSvg className={className} />;
}
